<% u = User.find(person['login']) %>

<a href='/users/<%= u.login %>'>
  <img src='<%= person['avatar_url'] %>' 
       alt='<%= u.login %>' 
       title='<%= u.login %>' 
       class="img-rounded user <%= "contributor" if u.contributor %>" />
</a>