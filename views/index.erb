<table class='table'>
  <tr>
    <th>Title</th>
    <th>Agree</th>
    <th>Disagree</th>
    <th>State</th>
  </tr>
  <% @pull_requests.each do |pr| %>
    <tr class='<%= row_class(pr) %>'>
      <td><a href='/<%= pr.number %>'><%= pr.title %></a></td> 
      <td><%= pr.agree.count %></td>
      <td><%= pr.disagree.count %></td>
      <td><%= pr.state %></td>
    </tr>
  <% end %>
</table>